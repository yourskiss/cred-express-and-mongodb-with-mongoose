<!DOCTYPE html>
<html>
<head>
  <title>Add Teacher</title>
</head>
<body>
<%- include('partials/header') %>
<section class="sectionmin">
  <h1  class="mainheading">Create New</h1>
  <form class="formpart" action="/teachers/add" method="post">
    <% if (error) { %>
      <aside class="errormessage"><%= error %></aside>
    <% } %>
    <% if (success) { %>
      <aside class="successmessage"><%= success %></aside>
      <script>
        setTimeout(() => {
          window.location.href = '/teachers';
        }, 2000); 
      </script>
    <% } %>
    <div>
        <label>Fullname:</label>
        <input 
            type="text" 
            name="fullname" 
            value="<%= data.fullname || '' %>"
        />
    </div>
    <div>
        <label>Mobile:</label>
        <input 
            type="text" 
            name="mobile" 
            value="<%= data.mobile || '' %>"

        />
    </div>
    <div>
        <label>Email:</label>
        <input 
            type="test" 
            name="email" 
            value="<%= data.email || '' %>"
        />
    </div>
    <div>
        <label>Password:</label>
        <input 
            type="text" 
            name="password" 
        />
    </div>
    <div>
      <button type="submit">Submit</button>
    </div>
  </form>
</section>
<%- include('partials/footer', { title: 'EMP Management System' }) %>
</body>
</html>
